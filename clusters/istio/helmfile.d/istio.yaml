repositories:
- name: istio
  url: https://istio-release.storage.googleapis.com/charts

releases:
- name: istio-base
  namespace: istio-system
  chart: istio/base
  version: 1.20.3
  installed: true

- name: istiod
  namespace: istio-system
  chart: istio/istiod
  version: 1.20.3
  installed: true
  wait: true